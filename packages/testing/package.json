{
  "name": "@deriv-trader/testing",
  "version": "0.1.0",
  "description": "Shared test utilities and fixtures (no production side effects).",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc --build",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@deriv-trader/domain": "0.1.0"
  },
  "devDependencies": {
    "typescript": "6.0.3",
    "vitest": "5.0.1"
  }
}
